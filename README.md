# 📦 zorgy — простая утилита для резервного копирования

`zorgy` — это легковесная консольная утилита на Python, позволяющая быстро создавать резервные копии файлов и каталогов с возможностью архивации в формат `.tar.gz`.

Разработана с использованием библиотеки [Click](https://click.palletsprojects.com/) для удобного CLI-интерфейса.

---

## ✨ Основные возможности

- Резервное копирование **любого файла или каталога**.
- Автоматическое создание **уникального имени** с временной меткой (например: `etc_2025-12-12_14-30-00`).
- Поддержка **сжатия** в `.tar.gz` (опционально).
- Автоматическое создание директории назначения, если она отсутствует.
- Валидация входных путей: проверка существования, читаемости и типа.
- Цветной вывод состояния операции (успех/ошибка).

---

## 🚀 Установка и запуск

Убедитесь, что у вас установлен Python 3.7+.

```bash
# Клонируйте репозиторий (или просто скопируйте main.py)
git clone https://github.com/AetherDeS/zorgy.git
cd zorgy

# Запуск напрямую (без установки)
python main.py --help
```

> Утилита не требует сторонних зависимостей, кроме стандартной библиотеки Python.

---

## 🛠 Использование

```bash
python main.py backup --source /etc --destination ~/backups --compress
```

### Основные опции:

| Опция | Кратко | Описание |
|------|--------|--------|
| `-s`, `--source` | **обязательно** | Путь к файлу или каталогу для резервного копирования |
| `-d`, `--destination` | опционально | Каталог для сохранения копии (по умолчанию: `/var/zorgy/backups`) |
| `--compress` | флаг | Архивировать копию в `.tar.gz` |
| `--version` | — | Показать версию утилиты |

Примеры:

```bash
# Архивация /home/user в /backups с сжатием
python main.py backup -s /home/user -d /backups --compress

# Простое копирование файла без архивации
python main.py backup -s ~/document.pdf
```

> 💡 **Совет**: добавьте `backup.py` в `PATH` или создайте symlink для удобного вызова из любого места.
