from setuptools import setup find_packages

setup(
    name='zorgy',
    version='1.0.0',
    description='Утилита для бэкапа системы',
    author='AetherDeS',
    packages=find_packages(),
    entry_points={
    
    }

)
